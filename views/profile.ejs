<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/profile.css">
</head>

<body>
    <!-- Header -->
    <%- include('partials/header') %>

        <div class="container my-4">
            <div class="profile-container">
                <h1>User Profile</h1>

                <!-- Profile Picture Section -->
                <div class="profile-picture">
                    <% if (user.profile_picture) { %>
                        <img src="<%= user.profile_picture %>" alt="Profile Picture">
                        <% } else { %>
                            <img src="/uploads/default-profile.jpeg" alt="Default Profile Picture">
                            <% } %>
                </div>

                <!-- Profile Details Section -->
                <div class="profile-info">
                    <label>Username:</label>
                    <span class="info-value">
                        <%= user.username %>
                    </span>
                </div>

                <div class="profile-info">
                    <label>Email:</label>
                    <span class="info-value">
                        <%= user.email %>
                    </span>
                </div>

                <div class="profile-info">
                    <label>Bio:</label>
                    <span class="info-value">
                        <%= user.bio %>
                    </span>
                </div>

                <div class="profile-info">
                    <label>Created at:</label>
                    <span class="info-value">
                        <%= user.createdAt %>
                    </span>
                </div>

                <div class="profile-info">
                    <label>Updated at:</label>
                    <span class="info-value">
                        <%= user.updatedAt %>
                    </span>
                </div>


                <% if(loggedUser && user.id===loggedUser.id) { %>
                    <!-- Edit and Delete Buttons -->
                    <div class="text-center">
                        <a href="/profile/edit/<%= user.id %>" class="btn btn-success">Edit Profile</a>
                        <a href="/api/users/delete/<%= user.id%>" class="btn btn-danger"
                            onclick="return confirmDeletion()">Delete</a>
                    </div>
                    <% } %>
            </div>
        </div>

        <!-- Footer -->
        <%- include('partials/footer') %>

            <script>
                function confirmDeletion() {
                    return confirm("Are you sure you want to delete your profile? This action cannot be undone.");
                }
            </script>
</body>

</html>